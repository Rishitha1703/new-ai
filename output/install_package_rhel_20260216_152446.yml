---
- name: Install mysql on servers
  hosts: all
  become: yes
  gather_facts: yes
  
  tasks:
    # Update package cache (Debian/Ubuntu)
    - name: Update apt cache (Debian/Ubuntu)
      apt:
        update_cache: yes
      when: ansible_os_family == "Debian"
      
    # Install on Debian/Ubuntu
    - name: Install mysql on Debian/Ubuntu
      apt:
        name: mysql
        state: present
      when: ansible_os_family == "Debian"
      
    # Install on RHEL/CentOS/Rocky/Alma
    - name: Install mysql on RHEL/CentOS
      yum:
        name: mysql
        state: present
      when: ansible_os_family == "RedHat"
      
    # Install on Fedora
    - name: Install mysql on Fedora
      dnf:
        name: mysql
        state: present
      when: ansible_os_family == "Fedora"
      
    # Verify installation (works on all)
    - name: Verify mysql is installed
      command: which mysql
      register: package_check
      changed_when: false
      failed_when: false
      
    - name: Display success message
      debug:
        msg: "✓ mysql installed successfully on {{ ansible_distribution }} {{ ansible_distribution_version }}"
      when: package_check.rc == 0


